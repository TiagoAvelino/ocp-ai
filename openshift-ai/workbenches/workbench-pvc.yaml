apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fine-tuning
  namespace: fine-tuning
  labels:
    opendatahub.io/dashboard: "true"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi
