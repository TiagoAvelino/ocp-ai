apiVersion: v1
kind: Service
metadata:
  name: ocp-mcp-server
  namespace: agent-demo
  labels:
    app: ocp-mcp-server
spec:
  selector:
    app: ocp-mcp-server
    deployment: ocp-mcp-server
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
